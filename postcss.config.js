
module.exports = {
    plugins: [
      require('autoprefixer'), // Adiciona prefixos de navegador automaticamente
      require('tailwindcss'),  // Integração com o Tailwind CSS (se estiver usando)
      // Outros plugins do PostCSS podem ser adicionados aqui conforme necessário
    ]
  };